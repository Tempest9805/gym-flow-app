/**
 * Application-wide constants.
 */

export const APP_NAME = 'Gym Flow';

/** Minimum touch target size in points (accessibility) */
export const TOUCH_TARGET_MIN = 44;

/** Role display labels */
export const ROLE_LABELS: Record<string, string> = {
  user: 'Athlete',
  trainer: 'Trainer',
  coach: 'Coach',
};
